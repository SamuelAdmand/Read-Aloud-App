name: Auto Release

on:
  push:
    tags:
      - 'v*' # Triggers whenever you push a tag like v1.0.1, v2.0, etc.

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write # This gives the robot permission to create the release
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'
          cache: 'gradle' # This saves your Android libraries for next time

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip' # This saves your Python libraries for next time

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build APK
        run: ./gradlew assembleDebug # Building the "Debug" APK because it's easier to share without complex signing keys

      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          files: app/build/outputs/apk/debug/*.apk # This uploads all generated APKs (universal + ABIs)
          name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
